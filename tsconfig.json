{
    "compilerOptions": {
        "module": "commonjs",
        "target": "es2015",
        "strict": true,
        "noImplicitReturns": true,
        "sourceMap": true,
        "outDir": "./target/",
        "rootDir": "./src/",
        "plugins": [{ "name": "tslint-language-service" }],
        "lib": [
            // `dom` lib is included by default. We override the `lib` option in order to exclude
            // it.
            "es2015"
        ]
    },
    "files": ["./src/index.ts"]
}
